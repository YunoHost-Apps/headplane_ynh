### Authentication keys

The authentication key for `__DEX__` will expire after __API_KEY_EXPIRES_DATE__.
The authentication key for `__HEADSCALE__` will expire after __PREAUTH_KEY_EXPIRES_DATE__.

A simple app (forced-)upgrade will renew them if needed.