
test_format = 1.0

[default]

    # ------------
    # Tests to run
    # ------------

    preinstall = """
    #sudo yunohost tools update apps
    #sudo apt update
    #sudo apt install python3-pycurl python3-bs4 -y
    """

    # -------------------------------
    # Default args to use for install
    # -------------------------------

    args.headscale_url = "https://headscale.domain.tld"
      # -------------------------------
    # Curl tests to validate that the app works
    # -------------------------------
    [default.curl_tests]
    #base_url = "__DOMAIN__/admin"
    #path = "/admin"
    home.path = "/admin"
    #admin.path = "/admin/login"
    admin.logged_on_sso = true
    #auto_test_assets = true
    #asset.path = "/core/img/logo/logo.svg"
    #file.path = "/remote.php/dav/files/__USER__/Readme.md"
    #file.logged_on_sso = true
    #file.expect_content = "# Welcome to Nextcloud!"
