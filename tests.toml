
test_format = 1.0

[default]

    # ------------
    # Tests to run
    # ------------

    preinstall = """
    sudo yunohost tools update apps
    sudo yunohost app install https://github.com/YunoHost-Apps/headscale_ynh/ -a "domain=$domain&init_main_permission=all…
    """

    # -------------------------------
    # Default args to use for install
    # -------------------------------

    args.headscale_url = "https://headscale.domain.tld"
